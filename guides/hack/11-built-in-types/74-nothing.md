The `nothing` type is a bottom type and has the exact same semantics as the [`noreturn`](/hack/built-in-types/noreturn) keyword. 
However, the two types behave differently with respect to type-checking.
